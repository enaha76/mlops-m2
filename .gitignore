# --- Secrets / credentials (per project spec: -1 pt per secret in repo) ---
params.yaml
.env
.env.*
!.env.example
*.pem
*.key
*_rsa
*_rsa.pub
.aws/
credentials

# --- Terraform ---
infra/terraform/.terraform/
infra/terraform/.terraform.lock.hcl
infra/terraform/terraform.tfstate
infra/terraform/terraform.tfstate.backup
infra/terraform/*.tfvars
!infra/terraform/*.tfvars.example
infra/terraform/crash.log
infra/terraform/crash.*.log

# --- DVC ---
# Exclude raw/processed data binaries, but KEEP:
#   - *.dvc pointer files (grille B1 requires them in git)
#   - .gitignore files DVC auto-places inside data/
/data/**
!/data/**/*.dvc
!/data/**/.gitignore
.dvc/tmp
.dvc/cache
.dvc/plots

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- MLflow local ---
mlruns/
mlartifacts/
mlflow.db

# --- OS / IDE ---
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# --- Build artifacts ---
dist/
build/
*.log

# --- Course reference (professor's spec, not a deliverable) ---
projet_final_mlops.pdf
*.pdf
